# assets/

This folder holds static local assets directly referenced in the React components bundle:
- Styling stylesheets (`global.css`, animations layout parameters).
- Static images (logos, avatar placeholders).
