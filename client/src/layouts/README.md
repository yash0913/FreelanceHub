# layouts/

This directory will contain shell layouts that define the overall UI grid frame (e.g., Sidebar, Navbar, Footer) for different sections of the application:
- `AuthLayout.jsx` - Simple centered frame for login/signup.
- `MainLayout.jsx` - Main workspace navigation header for regular customer and freelancer dashboards.
- `AdminLayout.jsx` - Admin-specific side navigation dashboard.
