# routes/

This folder handles frontend routing configurations using React Router:
- `AppRoutes.jsx` - Main router map. Defines public routes, authentication route protection guards (e.g., `ProtectedRoute`), and nests paths inside layout structures.
