# components/

This folder will house reusable, stateless UI components such as buttons, cards, modals, table views, loading indicators, and form input controls.
These components should be configurable via React props and decoupled from specific page/controller states.
