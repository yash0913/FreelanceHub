# context/

This folder will store React Context files providing global application states:
- `AuthContext.jsx` - Manages current authenticated user state, loading status, login/logout handlers, and token persistence in localStorage.
- `ToastContext.jsx` - Manages global notification toast states (success, warnings, errors).
