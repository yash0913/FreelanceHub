# config/

This folder will house environmental configurations:
- `db.js` - Configures connection pool logic or exports custom PrismaClient instances.
- `auth.js` - Stores constants like JWT expiration parameters and salt rounds.
